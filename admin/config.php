<?php
$host = "localhost";
$user = "root";
$password = "";
$db = "web";

$config=mysqli_connect($host, $user, $password);
if($config){
    $blok= mysqsli_select_db($config, $db);
    if (!$buka) {
    }  else {
    }
}
?>